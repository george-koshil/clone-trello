import React from "react";
import Checkbox from '@material-ui/core/Checkbox';

function Card({text}) {
    return(
        <div className='Card'>
            {text}

        </div>
    )
}

export default Card